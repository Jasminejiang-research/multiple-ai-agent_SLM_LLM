# 诊断来源快照

这些.txt文件保存本轮一次性诊断与校验脚本的源代码，不是产品入口。诊断脚本在可写暂存目录的tmp/下运行；保留.txt后缀可避免作为应用模块导入。各原生请求和响应仍以独立真实run目录中的events.jsonl为准。未运行的诊断mode不算测试证据。

若需复现一次性诊断，应在独立临时目录恢复脚本路径，使用新的输出目录和现有项目环境；未知lease必须先核对所属进程/端口并保留归档证明，不能删除历史来重跑。正常S3入口与命令见上一层HANDOFF.md。公共预算调整仍等待用户确认。

# A–D 两阶段生成：已批准的协议修订

授权：用户于 2026-09-07 对 s3-reference-fix-v3/NEXT_STEP_PROPOSAL.md 明确表示“批准该方案，请执行”。本目录为新版本，不覆盖三轮引用修复或最初能力测试历史。

共享生成协议：body-then-grounding-v1；contract=proposal-grounding-v5-two-stage；prompt=grounded-generation-v5-two-stage。此前 v4 为已撤回的字段别名试验，因此本次使用 v5。A 的 workflow 版本为 single-agent-common-contract-v2-two-stage，B/C/D 为 multi-agent-review-gates-v3-two-stage。

每个生成逻辑单元先生成正文/分析/完整财务值的内部草稿，验证字段和财务规则，再锁定草稿。第二阶段生成逐父节点的声明、评分和财务引用，并从闭集选择正文/来源片段。正文候选保留路径与字符范围；来源候选保留 chunk、行范围、hash 与原始摘录。组装仅复制所选原文和来源元数据，不计算新的财务值、不生成或改写正文，不替模型选择证据。

第二阶段不能提交改写后的正文、缺失父节点或跨父节点的正文片段。原 canonical schema、来源/inline citation/逐字引用、财务、版本及 lineage 校验全部保留。选择存在不代表证据在语义上支持声明；Gold 和六维人工评分依旧独立。未知证据选择仍为致命引用污染，不能以修复删除痕迹。

两个阶段共享一个逻辑任务 ID、原节点时钟和一次结构修复额度。正常调用数 A/B/C/D=8/15/18/18；Critic 仍单阶段。条件 Revision 数量不增加，被修订单元同样两阶段；全角色都发生 Revision 时，C/D 为32调用（未计repair/transport）。第一阶段通过时，逻辑任务仍未完成、首次完整契约仍未通过。第二阶段传输失败后重试成功，也不会回改首次通过状态。

每个物理请求记录 generation_stage、实际 schema hash、原始响应和真实 provider usage。逻辑任务额外保存冻结草稿、完整候选目录、选择到 canonical 字段的展开映射和最终 hash。展开文本不计入模型生成 tokens。事件新增字段有 single 默认值，旧日志仍可读取。

为保持原预算，候选目录按路径和范围去重，unsupported 集合只指向实际正文，不把财务字段名/ID/单位当作正文候选。提示词来源目录用可验证的位置引用已经完整提供的冻结证据；审计目录保留完整原文/hash。两个案例的合成18请求演练通过，当前最大输入加8192输出仍不超过32K，字符不超过90000。合成长度并不保证真实生成的更长上游一定合规；实际每次请求继续执行原字符/token/剩余预算门。

真实运行仅为冻结 AI 教育输入的非正式 SLM 测试：原 Granite CPU/32K/温度0，8192单次输出，累计36请求/500000tokens/14400秒。承接 s3-reference-fix-v3/run_01/probe/result.json 的累计11请求、71252tokens、4487.671秒。先单测 Research，完整验收通过才执行全新完整 D。未增加探针模型请求，既有原生数组上限证据保留。

正式公共配置文件保持原值；本次未授权启动 S5 正式八槽实验。新协议代码和契约需要在正式预检完成后重新统一冻结，旧单阶段产物不能混入新协议正式比较。原未提交文件在安装前逐一校验并备份；不 commit、不覆盖原始运行数据。
